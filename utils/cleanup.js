const constants = require('./generator-constants');

module.exports = {
    //cleanupOldFiles,
    //cleanupOldServerFiles
};
/**
 * Removes files that where generated in previous versions and therefore need to be removed
 */
function cleanupOldFiles(generator, javaDir, testDir) {

}

/**
 * Removes server files that where generated in previous versions and therefore need to be removed
 */
function cleanupOldServerFiles(generator, javaDir, testDir) {

}
